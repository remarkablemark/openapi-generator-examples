{
  "scripts": {
    "init": "fern init --openapi ../openapi.yaml",
    "generate": "fern generate --local --log-level debug",
    "clean": "rm -rf sdks/"
  },
  "dependencies": {
    "fera-api": "^3.79.2"
  }
}
